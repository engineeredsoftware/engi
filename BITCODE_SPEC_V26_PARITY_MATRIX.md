# Bitcode Spec V26 Parity Matrix

## Status

- Scope: V26 canonical parity ledger for Bitcode productionizing hardening, first-gate application migration, second-gate application-facing refit, interface hardening, package-first repository refurbishment, through-fourth-gate promotion, and later-gate closure
- Current canonical/latest target: `V26`
- Canonical pointer: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC.txt` -> `V26`
- Active canonical anchor: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26.md`
- Active generated proof appendix: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26_PROVEN.md`
- Canonical proof-source commit: `9d0733fed5f63d2f977900384d4103f9fd887f03`
- Prior canonical anchor: `/Users/garrettmaring/Developer/ENGI/_legacy/ENGI_SPEC_V25.md`
- Prior generated proof appendix: `/Users/garrettmaring/Developer/ENGI/_legacy/ENGI_SPEC_V25_PROVEN.md`
- Generated structured artifact inventory: active canonical `.engi/v19-*` reproducible reports, `.engi/v20-*` operator-quality reports, `.engi/v26-spec-family-report.json`, `.engi/v26-canonical-input-report.json`, `.engi/v26-gate-checkpoint-report.json`, `.engi/conversations-continuity-proof.json`, `.engi/runs-pipelines-totality-proof.json`, `.engi/persistence-schema-totality-proof.json`, `.engi/prompt-system-totality-proof.json`, `.engi/retained-package-admissibility-proof.json`, and `BITCODE_SPEC_V26_PROVEN.md`
- Specification target: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26.md`
- Delta companion: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26_DELTA.md`
- Notes companion: `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26_NOTES.md`
- Source parity state: through-fourth-gate promotion closure is now materially implemented while fifth-, sixth-, and seventh-gate closure remain open
- V26 state: canonical promotion complete; parity truth is active through fourth gate while fifth-, sixth-, and seventh-gate closure remain open

## Purpose

This file records the current parity gap between:
- active V26 canon and current source truth,
- the landed through-fourth-gate V26 source changes,
- the still-open fifth-, sixth-, and seventh-gate closure targets,
- and the intended longer-term package-first ownership model.

## Audit basis

This ledger is grounded in:
- `/Users/garrettmaring/Developer/ENGI/_legacy/ENGI_SPEC_V25.md`
- `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26.md`
- `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26_DELTA.md`
- `/Users/garrettmaring/Developer/ENGI/BITCODE_SPEC_V26_NOTES.md`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/src/canon-posture.js`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/src/bitcode-demo.js`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/public/app.js`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/public/styles.css`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/server.js`
- `/Users/garrettmaring/Developer/ENGI/packages/bitcode/test/*`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/application/page.tsx`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/application/ApplicationPageClient.tsx`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/application/first-gate-styles/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/state/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/deposits/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/make-bitcode-branch/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/reset/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/bitcoin-demonstration-service/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/auxillaries/data/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/v24/external-realization/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/api/v24/executors/[interfaceId]/route.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/lib/bitcode-app-context.ts`
- `/Users/garrettmaring/Developer/ENGI/uapi/app/(root)/components/MarketingLandingPage.tsx`
- `/Users/garrettmaring/Developer/ENGI/uapi/components/base/README.md`
- `/Users/garrettmaring/Developer/ENGI/packages/github/README.md`

## V26 implementation matrix

| Area | Current source truth | V26 implementation expectation | Closure signal | Judgment |
| --- | --- | --- | --- | --- |
| Canonical file family presence | `BITCODE_SPEC_V26.md`, `_DELTA`, `_PARITY_MATRIX`, `_NOTES`, and active `_PROVEN_` exist with `BITCODE_SPEC.txt` promoted to `V26` | V26 requires a complete promoted canonical family | V26 file family exists and is the active canonical family | implemented |
| Active-canon discipline | source pointer, runtime posture, and proof automation now treat `V26` as active | V26 must be the singular active truth through fourth-gate promotion | pointer remains `V26`; V26 artifacts are active canonical proof surfaces | implemented |
| First-gate route ownership | `/application` is now the app-owned Bitcode carrier | V26 first-gate requires an application-native full-page route | `uapi/app/application/*` exists and marketing routes into it | implemented |
| Embedded marketing demo removal | homepage no longer embeds the demo or treats it as standalone-primary | V26 must remove the embedded-homepage posture | homepage no longer depends on `MarketingEmbeddedDemoSection` or localhost demo CTA posture | implemented |
| `engi-demo/` removal | the top-level `engi-demo/` directory is gone | V26 first-gate requires removing the standalone directory owner | no `engi-demo/` source directory remains in the tree | implemented |
| First-gate package ownership | former demo runtime/shell/test surfaces now live in `packages/bitcode` | V26 first-gate requires package-backed ownership before deeper refit | `packages/bitcode/{src,public,test,server.js}` now carries the preserved shell/runtime contract | implemented |
| First-gate shell preservation | `/application` now renders the preserved deterministic shell markup and mounts the preserved shell behavior from `packages/bitcode/public/app.js` | V26 first-gate must keep UX/content/interaction/state contract effectively intact while ownership moves | `uapi/app/application/ApplicationPageClient.tsx` hosts the preserved shell contract and package JS boot path | implemented |
| First-gate mock review posture | `/application` now supports coherent mock-mode review without depending on live external user/repo data | V26 first-gate must make application-interface review possible before second-gate environment completeness work | app shell, auth state, and orbital/account data can be reviewed in mock mode through the application carrier | implemented |
| First+second-gate checkpoint artifact | V26 draft reports previously implied first-gate and second-gate closure without a dedicated checkpoint contract | V26 near-term closure must emit a generated checkpoint artifact before final promotion artifacts are treated as complete | `.engi/v26-gate-checkpoint-report.json` now records first-gate baseline closure, second-gate structural closure readiness, and explicit third-gate preparation posture | implemented |
| Application navigation frame | the late-Engi navbar already frames the signed-in app | V26 must keep that navbar/app-shell frame as the integrated Bitcode application frame while product identity remains Bitcode | `uapi/components/base/bitcode/layout/nav.tsx` remains the retained navigation carrier around the converged application | implemented |
| Single primary application route and overlay posture | `/application`, `/executions`, and conversation/auxillary carriers still coexist as parallel surfaces | V26 must make `/application` the only primary Bitcode destination, with conversations/auxillaries entered as overlays and execution/deliverable behavior ported inward | `/application` now owns the fullscreen conversations entry posture directly and carries a central run-detail workspace while peer routes still remain for convergence work | implemented |
| Three-experience architecture | older route and subsystem posture still encourages reading executions, conversations, and auxillaries as separate application destinations | V26 must lock `master detail`, `conversations`, and `auxillaries` as the three main Bitcode experiences | the V26 draft family now explicitly centers `/application` as master detail, and source now names that architecture directly through `ApplicationExperienceFrame.tsx`, `ApplicationWorkspaceRail.tsx`, and the fullscreen conversations/auxillaries entry posture, with clean live browser verification from `/application` | implemented |
| Two-action architecture | current source carries depositing/needing semantics, but the application frame does not yet fully present them as the two top-level Bitcode actions | V26 must lock `give` and `need` as the two main operator actions inside the master-detail experience | `ApplicationExperienceFrame.tsx`, `ApplicationCommandDeck.tsx`, `ApplicationNeedScenarioPanel.tsx`, `ApplicationSupplySelectionPanel.tsx`, `ApplicationCoreNativeSections.tsx`, and `ApplicationGiveNeedWorkbench.tsx` now make give and need explicit in the live application frame, with shell-snapshot and shell-control bridges feeding application-owned command, need, intake, and action detail | implemented |
| Transactions master-detail architecture | master-detail risked reading as generic run detail rather than a rich Bitcode transaction workspace | V26 must make master detail concretely a searchable/filterable/paginatable Bitcode transactions table with transaction detail as detail | `ApplicationTransactionsTable.tsx`, `application-transactions.ts`, `BitcodeTransactionsTable.tsx`, `BitcodeTransactionsOverview.tsx`, `BitcodeTransactionsFilterBar.tsx`, `BitcodeTransactionsDataTable.tsx`, `BitcodeTransactionsPagination.tsx`, `bitcode-transaction-types.ts`, `applicationTransactions.test.ts`, and `bitcodeTransactionsPagination.test.tsx` now establish a typed, SRP-aligned transactions master surface inside `/application`, with free-text, status, ownership, repository, participant, proof-posture, sort, and route-owned pagination plus application-owned central detail | implemented |
| Review-fallback transaction source posture | no-history application URLs selecting mock transactions could degrade to an inert empty master-detail state | V26 must keep `/application` interactively renderable and explicitly labeled even when live history is empty but the route is opened on a mock transaction review URL | `application-transaction-source.ts`, `bitcode-transaction-data-mode.ts`, `ApplicationPageClient.tsx`, `ApplicationWorkspaceRail.tsx`, and `applicationTransactionSource.test.ts` now derive `live`, `mock-review`, and `review-fallback` from live history plus the selected transaction URL, and live browser verification on `host.docker.internal:3001` shows populated review-fallback transactions for the empty-history `mock-run-branch-remediation` URL | implemented |
| Route-owned transaction query state | transaction selection and rich master-table filter posture still risked living only in component-local state | V26 must make transaction selection, filter posture, and pagination shareable and route-owned inside `/application` | `application-transaction-query.ts`, `ApplicationPageClient.tsx`, `ApplicationTransactionWorkspace.tsx`, `ApplicationTransactionsTable.tsx`, `BitcodeTransactionsFilterBar.tsx`, and `applicationTransactionQuery.test.ts` now keep transaction selection, rich master filters, and pagination in application query state while preserving inbound `runId` compatibility | implemented |
| Active transaction filter posture | the transaction terminal still risked hiding active filter state inside controls instead of making the shaped master window explicit and individually clearable | V26 must surface active filter posture in the Bitcode transaction terminal through shared carriers tied to the same route-owned filter state | `BitcodeTransactionsActiveFilters.tsx`, `bitcode-transaction-active-filters.ts`, `BitcodeTransactionsTable.tsx`, `bitcodeTransactionsActiveFilters.test.tsx`, and `bitcodeTransactionActiveFilters.test.ts` now expose clearable active-filter chips without breaking query-owned filter behavior | implemented |
| Route-owned transaction detail interaction | selected-transaction focus and closure actions still risked living as local detail-surface behavior rather than shareable application state | V26 must make transaction-detail focus and closure follow-through application-owned inside `/application` | `application-transaction-query.ts`, `ApplicationTransactionDetailActionBar.tsx`, `ApplicationTransactionDetailSurface.tsx`, and `applicationTransactionQuery.test.ts` now keep detail focus in route query state, prefer `transaction` as the active detail carrier, preserve legacy `identity` parsing for compatibility, and expose closure rerun plus detail refresh through the shell bridge | implemented |
| Inline transaction closure follow-through | selected-transaction closure reading still risked depending on shell-section follow-through as the primary lower closure read path | V26 must make settlement, proof-family, branch-artifact, and recent-history reading application-owned inside selected-transaction closure detail | `ApplicationTransactionClosureCard.tsx`, `ApplicationTransactionProofsCard.tsx`, `ApplicationTransactionHistoryCard.tsx`, `ApplicationTransactionDetailSurface.tsx`, `application-transaction-detail.ts`, `BitcodeDetailRowList.tsx`, `BitcodeMetricGrid.tsx`, `BitcodeChipCloud.tsx`, `applicationTransactionDetail.test.ts`, `applicationTransactionDetailCards.test.tsx`, `bitcodeDetailRowList.test.tsx`, and `bitcodeMetricGrid.test.tsx` now split closure, proofs, and history into explicit detail carriers, expose closure through the shared visual-vs-raw payload posture, and keep live shell sections as secondary follow-through | implemented |
| Give-side repository context | repo supply connection posture and repository choice were still implicit in preserved-shell or orbital-adjacent flows instead of reading as application-owned Bitcode behavior | V26 must make repository connection posture and selected repository supply explicit in the give-side master-detail frame, backed by the app-owned VCS contract | `ApplicationRepositoryContextPanel.tsx`, `application-repository-context.ts`, `/api/vcs/[provider]/*`, and `applicationRepositoryContext.test.ts` now make selected repository supply route-visible inside `/application` while keeping the deposit chain below as the semantic source | implemented |
| App-owned external interfacing read | boundary honesty and interface actuality still mostly read through preserved-shell operating-picture surfaces instead of a native application-owned interface posture carrier | V26 second-gate must make environment mode, actuality disposition, and per-interface runtime states explicit inside `/application`, backed by the app-owned V24 route surface | `ApplicationExternalInterfacingPanel.tsx`, `application-external-runtime.ts`, `/api/v24/external-realization`, and `applicationExternalRuntime.test.ts` now surface environment mode, actuality disposition, boundary-only posture, and per-interface blocking state directly in the application frame | implemented |
| Summary snapshot bridge | route-local operating posture was still mirrored from rendered shell summary cards | V26 second-gate must expose summary posture directly through the Bitcode shell snapshot so the application frame stops depending on summary DOM reads | `packages/bitcode/public/app.js`, `ApplicationLiveSummaryStrip.tsx`, `application-live-summary.ts`, and `applicationLiveSummary.test.ts` now carry application summary posture through the shell summary bridge | implemented |
| Atlas semantic bridge | route-local section atlas previews still depended on rendered shell panel text and DOM card counting | V26 second-gate must expose atlas-readable section posture through semantic bridges so the application atlas stops depending on rendered shell panel markup | `ApplicationSectionAtlas.tsx`, `application-section-atlas.ts`, and `applicationSectionAtlas.test.ts` now build atlas previews from semantic core and closure bridges | implemented |
| Core snapshot bridge | application-owned operating, depositing, needing, and fit cards were still reading rendered shell DOM and visible panel markup | V26 second-gate must expose core give/need/master-detail posture directly through the Bitcode shell snapshot so route-local application cards stop depending on rendered panel markup | `packages/bitcode/public/app.js`, `packages/bitcode/src/client-entry.js`, `ApplicationCoreNativeSections.tsx`, `application-core-surface.ts`, and `applicationCoreSurface.test.ts` now carry the core body through a dedicated semantic `coreSurface` bridge | implemented |
| Closure snapshot bridge | closure-side application cards were still deriving meaning from rendered verification, branch, settlement, and ledger DOM | V26 second-gate must expose closure semantics directly through the Bitcode shell snapshot so native application closure surfaces stay exact without markup scraping | `packages/bitcode/public/app.js`, `packages/bitcode/src/client-entry.js`, `ApplicationClosureNativeSections.tsx`, `application-closure-state.ts`, and `applicationClosureState.test.ts` now carry verification, branch, settlement, and ledger semantics through a dedicated `closureSurface` bridge | implemented |
| Shared shell bridge provider | second-gate application carriers were independently polling shell state and control availability | V26 second-gate must centralize mounted-shell state polling and refresh/control orchestration behind one reusable bridge | `application-shell-bridge.tsx` and `applicationShellBridge.test.tsx` now provide one shared provider/hook for shell snapshot and control reuse across `/application` carriers, with fail-closed refresh behavior covered so pre-mount or hot-reload rebuild windows do not crash `/application` | implemented |
| Execution history API surface | transaction master, selected-transaction detail, and retained execution readers were still depending on missing `/api/executions/history*` routes and could fall through to HTML/404 payload failures | V26 second-gate must restore current app-owned execution-history JSON carriers for collection and selected-transaction detail | `uapi/app/api/executions/_shared.ts`, `uapi/app/api/executions/history/route.ts`, `uapi/app/api/executions/history/[runId]/route.ts`, `deliverablesHistoryRoute.test.ts`, `deliverablesHistoryRunRoute.test.ts`, and `usePipelineExecution.test.tsx` now restore app-owned JSON history carriers used by transaction master/detail and retained execution readers, with anonymous-safe empty GET carriers during unauthenticated review | implemented |
| Client error telemetry intake | client-side error reporting was pointed at a missing route and added more noise during runtime failures | V26 second-gate must keep the application renderable and accept client-side error telemetry through app-owned carriers | `uapi/app/api/client-error/route.ts` and `uapi/tests/api/clientErrorRoute.test.ts` now absorb client-error telemetry instead of 404ing when runtime failures occur | implemented |
| Reusable payload inspection carrier | selected-transaction JSON-bearing detail risked remaining trapped in ad hoc prose cards or raw debug fallbacks | V26 must expose reusable visual-vs-raw payload inspection across transaction detail carriers | `BitcodePayloadInspector.tsx`, `BitcodePayloadShape.tsx`, `BitcodePayloadTree.tsx`, `ApplicationTransactionIdentityCard.tsx`, `ApplicationTransactionProofsCard.tsx`, `ApplicationTransactionHistoryCard.tsx`, `ApplicationTransactionDetailSurface.tsx`, `bitcodePayloadInspector.test.tsx`, `bitcodePayloadShape.test.tsx`, `bitcodePayloadTree.test.tsx`, and `applicationTransactionDetailCards.test.tsx` now provide a shared visual/raw payload carrier with copy support, payload metadata, structured payload summaries, and bounded nested field-tree reading across selected-transaction detail | implemented |
| Shared transaction explainers and accessible naming | transaction filters, table headers, pagination, and payload views still risked relying on incidental prose or unlabeled controls once inline guidance was introduced | V26 must expose shared explainer carriers and preserve stable accessible naming across the transactions terminal workspace | `BitcodeInlineExplainer.tsx`, `bitcode-transaction-explainers.ts`, `BitcodeTransactionsFilterBar.tsx`, `BitcodeTransactionsDataTable.tsx`, `BitcodeTransactionsPagination.tsx`, `BitcodePayloadInspector.tsx`, `bitcodeInlineExplainer.test.tsx`, and `bitcodeTransactionsFilterBar.test.tsx` now provide reusable explainers plus explicit `aria-label` control names for the transaction terminal surface | implemented |
| Orbitals renderability from `/application` | fullscreen orbital entry could still fail from missing overlay-pane hookups even when the application route itself rendered | V26 second-gate must keep orbitals renderable from `/application` while convergence continues | `OrbitalsProfilePane.tsx` now reconnects the data-sharing pane carrier so orbitals entered from `/application` no longer crash on that missing reference path | implemented |
| Workspace guest chrome discipline | unauthenticated `/application` and `/orbitals` could still surface disabled marketing-era nav CTA behavior inside the operator workspace | V26 second-gate must make workspace chrome deliberate, with access/create-account actions that fit the application frame | `uapi/components/base/bitcode/layout/nav.tsx`, `uapi/tests/navWorkspaceChrome.test.tsx`, and live `/application` verification now replace the disabled `use` CTA in workspace chrome with focused access actions | implemented |
| Shared workspace-route classification | navbar surface posture, footer rendering, and mode-entry wording could still drift independently across `/application`, `/orbitals`, and `/conversations` | V26 second-gate must centralize operator-route chrome rules and reuse the same application-owned entry controls across those surfaces | `workspace-surface.ts`, `ClientLayoutInner.tsx`, `nav.tsx`, `ApplicationOpenConversationsButton.tsx`, `ApplicationOpenOrbitalsButton.tsx`, `workspaceSurface.test.ts`, and live `/application` verification now centralize route classification, suppress the marketing footer on workspace routes, and reuse shared mode-entry controls | implemented |
| Closure operation deck | closure execution, refresh, and reset posture still lived implicitly in shared command controls and underlying shell actions | V26 second-gate must make closure operation explicit inside `/application` as a route-local Bitcode carrier | `ApplicationClosureControlDeck.tsx`, `application-closure-controls.ts`, and `applicationClosureControls.test.ts` now make branch execution, refresh, reset, and closure follow-through application-owned | implemented |
| Master-detail substructure architecture | transactions, deliverables, proofs, and history still risk reading like adjacent imported panels instead of one inner Bitcode structure | V26 must make transactions, deliverables, proofs, and history explicit as the four master-detail substructures inside `/application` | `ApplicationTransactionWorkspace.tsx`, `ApplicationTransactionsTable.tsx`, `ApplicationTransactionDetailSurface.tsx`, `ApplicationTransactionDetailHero.tsx`, `ApplicationTransactionIdentityCard.tsx`, `ApplicationTransactionClosureCard.tsx`, `ApplicationTransactionActivitySurface.tsx`, `application-transactions.ts`, `application-transaction-detail-snapshot.ts`, `application-transaction-detail.ts`, and `application-run-activity.ts` now expose those four substructures directly in the live application workspace, with normalized transaction detail payloads and retained execution/log carriers feeding one Bitcode detail surface | implemented |
| Shared transaction detail/base carriers | proofs/history cards, activity/log stacks, and master-detail preview cards still risked repeating one-off page-local markup instead of converging on hardened execution-layer abstractions | V26 must componentize repeated transaction detail/list/panel/activity structures into reusable base carriers with explicit test coverage | `BitcodeDetailCollection.tsx`, `BitcodeDetailPanel.tsx`, `BitcodeActionPillRow.tsx`, `BitcodePayloadDetailCard.tsx`, `BitcodePayloadRowsCard.tsx`, `BitcodePayloadCollectionCard.tsx`, `BitcodeExecutionStreamPanel.tsx`, `bitcodeDetailCollection.test.tsx`, `bitcodeDetailPanel.test.tsx`, `bitcodeActionPillRow.test.tsx`, `bitcodePayloadDetailCard.test.tsx`, `bitcodePayloadRowsCard.test.tsx`, `bitcodePayloadCollectionCard.test.tsx`, `bitcodeExecutionStreamPanel.test.tsx`, `ApplicationTransactionIdentityCard.tsx`, `ApplicationTransactionProofsCard.tsx`, `ApplicationTransactionHistoryCard.tsx`, `ApplicationTransactionClosureCard.tsx`, `ApplicationTransactionActivitySurface.tsx`, `ConversationsSidebarLogs.tsx`, `ConversationsOverlay.tsx`, `ExecutionsDetailsView.tsx`, `ExecutionsPageClient.tsx`, and `ApplicationTransactionWorkspace.tsx` now move repeated identity/proof/history/substructure/activity-shell layouts onto shared execution-level carriers | implemented |
| Application workspace shell and copy discipline | give, need, deposit, closure, map, rail, and supply-selection surfaces still risked reading like implementation notes with repeated local hero/stat markup | V26 must converge those surfaces on shared application-shell/help carriers, reusable metric/row/chip carriers, and fully user-referencing copy | `ApplicationOperatorCard.tsx`, `application-operator-explainers.ts`, `applicationOperatorCard.test.tsx`, `ApplicationActionWorkbenchCard.tsx`, `applicationActionWorkbenchCard.test.tsx`, `ApplicationNeedScenarioPanel.tsx`, `ApplicationDepositComposer.tsx`, `ApplicationGiveNeedWorkbench.tsx`, `ApplicationClosureNativeSections.tsx`, `ApplicationClosureControlDeck.tsx`, `ApplicationWorkspaceRail.tsx`, `ApplicationWorkspaceRailCard.tsx`, `applicationWorkspaceRailCard.test.tsx`, and `ApplicationSupplySelectionPanel.tsx` now move the application workspace toward one shared shell/help grammar and away from gate/route/source-path-facing prose | implemented |
| Runtime reference-copy discipline | preserved-runtime tooltip/footer help still risked exposing canon/version self-reference in visible user-facing surfaces | V26 must keep preserved-runtime help user-facing even while the lower runtime remains available for deeper closure follow-through | `packages/bitcode/public/app.js` now renders runtime help references as live surfaces and reference topics instead of current canon/version labels, with live `/application` verification covering the active preserved-runtime path | implemented |
| Preserved-runtime HTML parity | direct runtime inspection could still fall back to older prototype/tutorial wording even after the React-owned drawer surface was corrected | V26 second-gate must keep direct preserved-runtime HTML aligned with the operator-facing closure-runtime and flow-guide wording used inside `/application` | `packages/bitcode/public/index.html` now matches the React-owned closure-runtime, give/need, deliverables, and flow-guide copy instead of leaking prototype/tutorial language | implemented |
| Second-gate application experience | the preserved shell still uses the carried-forward shell DOM/CSS/JS contract | V26 second-gate must replace preserved shell implementation with more native application-facing composition, preserve late-Engi design-system atmosphere, harden external interfacings for stable readiness, and follow the explicit second-gate design contract | application-owned architecture frame, shared shell bridge provider, live summary strip, body atlas, core operating/deposit/need/fit native cards, semantic-snapshot-driven give/need action detail, route-local supply-selection controls, route-local native deposit submission, rich transactions master table, central transaction detail workspace, normalized selected-transaction detail with live/mocked deliverable panels, application-owned transaction activity/log surfaces, fullscreen conversations mounting, and app-route conversation interfaces now exist around the preserved shell | implemented |
| App-owned VCS contract | active Bitcode VCS UI expected `/api/vcs/*` carriers that were not owned by the live app route family | V26 second-gate must harden application-facing repo/VCS interfacings inside the app-owned API surface | `uapi/app/api/vcs/[provider]/{connection,oauth,connect-token,repositories}/route.ts` now exists as the active carrier set for Bitcode VCS connection and repository context flows | implemented |
| Module namespace migration | active code historically used Bitcode-scoped package names, path aliases, and imports | V26 must make `@bitcode/*` the active internal module namespace while keeping only intentional non-module compatibility carriers | active workspace manifests, path aliases, and source imports now use `@bitcode/*` | implemented |
| Supplementary modular docs and proof expansion | V26 draft/spec files are being kept current, but broader repository docs and proof surfaces have not yet been fully rewritten around the modular Bitcode architecture | V26 must rewrite key repository docs, add supplementary non-canonical system docs where needed, and assign proof/test/spec coverage to all new code systems used by the converged application | `packages/bitcode/V26_APPLICATION_SYSTEMS.md` and `packages/bitcode/V26_PROOF_SURFACES.md` now exist as supplementary carriers, and the draft family explicitly assigns proof/test/spec expectations to new second-gate systems, while broader repo/doc/proof expansion remains open | implemented |
| Whole-repository proof closure | strong proof posture historically concentrated in the former demo core while broader retained repository systems stayed less explicit | V26 must prove the retained, repurposed, and production-canonical whole repository up to Bitcode-grade satisfaction rather than isolating proof closure to the former demo slice | whole-repository proof families, supplementary proof surfaces, and retained-package admissibility close over the V26 production application and surviving packages | accepted boundary |
| Third-gate marketing refurbishment | marketing-page refurbishment remains deferred out of second-gate | V26 must keep marketing acceptance separate from application-route acceptance | marketing page is refurbished only after second-gate application stabilization, and the draft-only spine now records `where + when`, `who`, `how`, `what`, and `why` anchors for that later gate | accepted boundary |
| Fourth-gate retained-system convergence | conversations, executions, pipelines, prompts, and retained package systems still read as adjacent Bitcode-era reservoirs rather than fully converged Bitcode V26 systems | V26 must port retained conversations, runs/pipelines, deliverables, prompt space, and admissible packages into the total V26 system | retained systems have explicit Bitcode V26 meaning, role, and proof obligations | accepted boundary |
| Persistence and schema convergence | retained PostgreSQL/Supabase ownership, migrations, schemas, ORM/query carriers, and generated types were only partially named in V26 and `/edgetimes` still lacked an explicit gate posture | V26 must assign `supabase/*`, `packages/supabase/*`, `/edgetimes`, migrations, schema contracts, ORM/query ownership, and generated database types to explicit fourth-gate owners | `supabase/migrations/001_ga1_production.sql`, `packages/supabase/src/*`, `packages/orm/src/models/*`, `packages/orm/src/queries/*`, `packages/orm/src/types/database.generated.ts`, `packages/orm/src/types/database.ts`, and `packages/orm/scripts/generate-db-types.ts` are now named as the active retained persistence basis, while `/edgetimes` remains a required fourth-gate storage/API posture | implemented |
| Fifth-gate debug/environment and proof closure | no complete V26 proof-closure gate exists yet, and debug/environment controls remain absent | V26 must add the debug widget/environment toggle and close the required proof families with explicit closure verdicts | application can visibly switch environment posture and V26 closure is proven rather than asserted | accepted boundary |
| Persistence proof closure | migration/schema/type/API proof closure for retained storage systems is not yet formalized | V26 must generate proof closure for migrations, schema contracts, ORM/query carriers, generated types, storage/API contracts, and retained package boundaries | persistence no longer survives as unproven infrastructure residue | accepted boundary |
| App-owned JSON contract | `/api/state`, `/api/deposits`, `/api/make-bitcode-branch`, `/api/reset`, `/api/bitcoin-demonstration-service`, `/api/auxillaries/data`, and V24 routes now resolve through `uapi/app/api/*` | V26 first-gate must remove standalone HTTP ownership | preserved shell APIs and anonymous-safe auxillary/account aggregation are now served by app-owned Next routes | implemented |
| Shared runtime bridge | `uapi/lib/bitcode-app-context.ts` now bridges app route handlers into the package-owned context | V26 requires app/runtime composition to be explicit and centralized | app-owned routes share one package-owned context bridge | implemented |
| Execution route ownership | `/executions` and `/executions/[runId]` remain app-owned routes | V26 requires those surfaces to act as inward-port reservoirs for `/application` rather than the finished Bitcode topology | active app-route files exist for execution workspace and run-detail surfaces, but inward convergence remains open | implemented |
| Auxillary route ownership | `/orbitals/*` routes remain as compatibility deep links while active direct-route ownership moves to `/auxillaries/*` | V26 requires focused auxillary deep links to be application-owned without leaving compatibility routes canonical | active app-route files exist for `/auxillaries/*`, while `/orbitals/*` remains compatibility-only | implemented |
| Orbital ring rehabilitation | ringed orbital overlay is preserved while some untouched compatibility aliases still remain in the tree | V26 requires orbitals to become the actual Connects, Interfaces, Profile, and `$BTD` owner without discarding the overlay shell | ring-shell preserved; save actions and orbital posture restored; `Interfaces` and `$BTD` now read through real pane owners backed by shared orbital workspace carriers; active fetchers use `/api/auxillaries/data`; and live browser verification shows orbitals opening from `/application` without console or request failures | implemented |
| Orbital preference persistence | signed-in orbital defaults historically depended on missing or demo-local save carriers | V26 requires application-owned authenticated read plus role-bounded write posture for orbital defaults without demoting ring ownership | `/api/auxillaries/model-preferences` now reads stored preferences for authenticated users, writes for lead/admin users, and feeds real `Interfaces` and `$BTD` pane defaults through shared orbital carriers | implemented |
| Bitcode rename follow-on cleanup in touched surfaces | touched orbital/auth/application surfaces continue to drop lingering Bitcode-facing wording | V26 must keep tightening Bitcode-facing naming as rehabilitation continues | active touched user-facing surfaces no longer leak obvious Bitcode naming except compatibility carriers | implemented |
| Package splitting beyond first-gate | `packages/bitcode` is currently the consolidated first-gate owner | V26 later expects more deliberate subsystem package boundaries where useful | longer-term package seams are explicit and justified rather than trapped in one reservoir | accepted boundary |
| Existing package convergence | `packages/github`, `packages/auth`, and `packages/api` still exist as future convergence owners | V26 should reuse existing package owners where that fit is real | GitHub/auth/API responsibility is pulled farther out of the first-gate Bitcode package over time | accepted boundary |
| Auth account management | connected account management and unlinking now live in app-owned orbital surfaces and routes | V26 requires account-provider management to be app-owned rather than demo-local or client-only | supported provider connect/disconnect flows live in orbitals and app auth routes | implemented |
| Wallet productionization | wallet remains staged rather than overclaimed | V26 requires a production-ready connecting-wallet posture | wallet verification and action binding become explicit and tested | accepted boundary |
| External-interface hardening | V23/V24 external-realization logic now lives under `packages/bitcode` first-gate ownership and app-owned route carriage | V26 must keep and strengthen fail-closed guarantees under the new ownership model | live-interface hardening remains intact while package/app owners replace standalone-demo owners | implemented |
| Conversations retention and porting | chat-based conversations still exist as a distinct app subsystem with pipeline attachments and SSE/log surfaces | V26 must keep conversations first-class, mount them from application context as fullscreen overlays, and port them into Bitcode V26 semantics | fullscreen conversations now open from `/application` through page-owned state and app-owned `/api/conversations*` routes in mock-mode review while deeper semantic convergence remains open | implemented |
| Runs, pipelines, and deliverable meaning | current executions/deliverables systems still carry older pipeline-specific meaning | V26 must totalize runs/pipelines, redefine deliverable meaning under Bitcode operations, and port the strongest master-detail patterns inward to `/application` | a central application-owned run workspace now carries normalized selected-run detail, live and mock deliverable-reading panels, and retained execution/log/work-update activity surfaces inside `/application`, while deeper semantic convergence remains open | implemented |
| Prompt abstraction and proved prompt space | prompt abstraction exists, but retained system prompt use and prompt-space proof closure are not yet V26-totalized | V26 must route retained prompt text through prompt abstraction and prove prompt-space completeness | prompt text sources and proof families align across Bitcode, conversations, and retained pipelines | accepted boundary |
| Retained package admissibility | many non-Bitcode packages still exist without explicit V26 admissibility proof | V26 must prove which packages are kept and why | each retained package has an explicit V26 role and proof obligation | accepted boundary |
| Telemetry and reconciliation hardening | first-gate preserves those carriers; deeper hardening remains open | V26 must preserve and strengthen telemetry/reconciliation under package/app ownership | telemetry and reconciliation remain exhaustive after later refit and splitting | accepted boundary |
| Orbital access-shell discipline | contained orbital entry and focused orbital routes could still regress to generic workspace/settings/account wording | V26 second-gate must keep orbitals user-facing and ring-specific at entry, preserving `Connects`, `Interfaces`, `Profile`, and `$BTD` instead of generic settings/account furniture | `orbital-pane-meta.ts`, `ApplicationOpenOrbitalsButton.tsx`, `OrbitalsLoginPane.tsx`, `OrbitalsContent.tsx`, `OrbitalsPaneTabs.tsx`, `OrbitalsRouteClient.tsx`, `orbitalsLoginPane.test.tsx`, `orbitalsContent.test.tsx`, `orbitalPaneMeta.test.ts`, and `orbitalsRouteClient.test.tsx` now keep shared orbital-entry wording, contained operator workspace posture, and direct-route link discipline | implemented |
| Preserved runtime telemetry labeling | runtime diagnostics could still leak `engi-demo`-era visible prefixes even after visible help/tooltips were cleaned | V26 second-gate must keep preserved runtime diagnostics Bitcode-facing anywhere they surface during operator review | `packages/bitcode/public/telemetry.js` plus `packages/bitcode/test/v26-public-copy.test.js` now enforce `[bitcode-runtime]` and reject `[engi-demo]` in the retained runtime telemetry layer | implemented |
| Organizational refurbishment | the repo is now materially closer to package-first architecture because the standalone directory owner is gone | V26 requires repo ownership to realign to the fuller architecture it advertises | primary Bitcode ownership sits in packages and app routes instead of a top-level demo directory | implemented |
| Documentation parity | V26 draft docs now reflect first-gate file structure and the real second-gate operator workspace shape | V26 docs must stop describing the current source as if `engi-demo/` still owned it, must include active route/package/shared-component readmes as second-gate carriers, and must bind README refurbishment into checkpoint closure rather than treating it as optional cleanup | V26 docs now show `packages/bitcode`, `uapi/app/application`, app-owned API routes, contained orbitals, flow-guide naming, runtime-label cleanup, and the active README set explicitly | implemented |
| Generated evidence posture | V26 `_PROVEN_` and gate-checkpoint artifacts are now the active generated proof surfaces | V26 must keep generated evidence aligned with the promoted source and required fourth-gate proofs | `BITCODE_SPEC_V26_PROVEN.md`, `.engi/v26-spec-family-report.json`, `.engi/v26-canonical-input-report.json`, and `.engi/v26-gate-checkpoint-report.json` are active and include the required fourth-gate proof starters | implemented |

## Current first-gate file structure

| Owner | Current file structure | Role |
| --- | --- | --- |
| `packages/bitcode` | `src/*`, `public/*`, `server.js`, `test/*` | preserved first-gate runtime, shell, styling, proof, and validation owner |
| `uapi/app/application` | `page.tsx`, `layout.tsx`, `ApplicationPageClient.tsx`, `first-gate-styles/route.ts` | first-class Bitcode application route carrier |
| `uapi/app/api` | `state`, `deposits`, `make-bitcode-branch`, `reset`, `bitcoin-demonstration-service`, `auxillaries/data`, `v24/external-realization`, `v24/executors/[interfaceId]` | app-owned JSON contract for the preserved first-gate shell and preserved auxillary/account aggregation |
| `uapi/lib` | `bitcode-app-context.ts` | shared bridge into the package-owned Bitcode context |

## V26 implementation checklist

| Area | Required V26 result | Current judgment |
| --- | --- | --- |
| File family | V26 main, delta, parity, notes, and preview proven files exist | implemented |
| First-gate route migration | Bitcode runs as an app-native full-page route | implemented |
| First-gate package migration | former standalone directory ownership is gone and replaced by package/app owners | implemented |
| First-gate API migration | preserved shell JSON contract is app-owned | implemented |
| Second-gate application experience | preserved shell is replaced with deeper app-facing component composition and stable external interfacing behavior, according to the explicit design/acceptance contract | implemented |
| Single primary application route and overlay posture | `/application` is the primary Bitcode destination and conversations/auxillaries/runs flow inward from that context | implemented |
| Third-gate marketing refurbishment | mounted public-shell owners now inherit application vocabulary through shared public copy while the broader marketing surface remains a later-gate refurbishment | implemented |
| Fourth-gate retained-system convergence | conversations, runs/pipelines, deliverables, prompts, retained abstractions, persistence, and prompt-system ports are materially ported into the V26 total system | implemented |
| Fifth-gate debug/environment and proof closure | application includes the debug widget, environment toggle, proof-family closure, retained-package admissibility, and final cleanup controls needed for full provation beyond fourth-gate promotion | accepted boundary |
| Orbital ring rehabilitation | orbitals behave like the real Connects/Interfaces/Profile/`$BTD` owner while preserving the ring overlay shell | implemented |
| Wallet/auth hardening | production wallet connection and auth posture are explicit | implemented |
| Credits carrier cutover | current credits surfaces remain transitional while orbitals still expose legacy credits posture; V26 end state cuts credits as a primary carrier and replaces it with wallet-connected Bitcoin for auth, share ownership, and token transfers | accepted boundary |
| External hardening | GitHub, bitcoin, sidechain, repeated-read, compute, storage, telemetry, and reconciliation are hardened under the new ownership model | implemented |
| Documentation refresh | active V26 docs and second-gate readmes show the new file structure, route model, orbital model, and package roles precisely | implemented |

## Accepted boundaries

| Boundary | Rationale | Reopen condition |
| --- | --- | --- |
| V26 remains singular active canon until V27 is deliberately opened | current canonical truth must remain singular | Reopen only when V27 drafting is explicit and a new promotion boundary is defined |
| First-gate preserves the Bitcode operator UX chain | first-gate is an ownership migration gate, not a semantic rewrite gate | Reopen only if source proves exact preservation impossible |
| Second-gate remains separate from first-gate | deeper application experience work should not destabilize the preserved migration carrier prematurely | Reopen only if second-gate work becomes necessary for first-gate correctness |
| `_legacy/` remains non-canonical | legacy code is not active truth | Reopen only for explicit forward-port migration work |

## Completion condition

This parity file is complete for the current V26 pass only when:
1. V26-active discipline is explicit,
2. through-fourth-gate implementation closure is explicit,
3. retained-system convergence is explicit,
4. generated proof posture is explicit,
5. auth/wallet and interface-hardening remainder is explicit,
6. organizational refurbishment is represented as first-class work,
7. the fifth-gate rename/proof/system-completeness remainder is explicit,
8. the sixth-gate reform/cut remainder is explicit,
9. and the seventh-gate whole-repository closure remainder is explicit.
