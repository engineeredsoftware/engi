import { PromptPart } from '../../parts/PromptPart';

/**
 * @doc-comment-developing-promptpartdevelopment
 * domain: agent
 * intent: "Agent-specific semantic unit (PROMPTPART_SPECIFIC_AGENT_LANGUAGE_TRY_PROCESSING_TECHNIQUES)"
 * current_version: "GA1.50.0"
 * versions: []
 * benchmarks: [
 *   { "name": "technical_accuracy", "test": "Uses concrete technical language", "score": 0.50 },
 *   { "name": "implementation_ready", "test": "Provides clear actionable guidance", "score": 0.50 }
 * ]
 */

import type { PromptPart } from '../../parts/PromptPart';

export const PROMPTPART_SPECIFIC_AGENT_LANGUAGE_TRY_PROCESSING_TECHNIQUES: PromptPart = 
  'Techniques: sentence segmentation, lemmatization, entity linking, dependency analysis, topic modeling sketches, extractive/abstractive summarization. Prefer deterministic approaches when accuracy requirements are strict.' as PromptPart;