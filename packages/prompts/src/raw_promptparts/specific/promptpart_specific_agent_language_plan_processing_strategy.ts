import { PromptPart } from '../../parts/PromptPart';

/**
 * @doc-comment-developing-promptpartdevelopment
 * domain: agent
 * intent: "Agent-specific semantic unit (PROMPTPART_SPECIFIC_AGENT_LANGUAGE_PLAN_PROCESSING_STRATEGY)"
 * current_version: "GA1.50.0"
 * versions: []
 * benchmarks: [
 *   { "name": "technical_accuracy", "test": "Uses concrete technical language", "score": 0.50 },
 *   { "name": "implementation_ready", "test": "Provides clear actionable guidance", "score": 0.50 }
 * ]
 */

import type { PromptPart } from '../../parts/PromptPart';

export const PROMPTPART_SPECIFIC_AGENT_LANGUAGE_PLAN_PROCESSING_STRATEGY: PromptPart = 
  'Define processing steps: tokenization/normalization, NER, keyphrase extraction, summarization, and reasoning depth. Choose tools and parameters suitable for the text domain and length.' as PromptPart;