/**
 * @doc-comment-developing-promptpartdevelopment
 * domain: agent
 * intent: "Agent-specific semantic unit (PROMPTPART_SPECIFIC_AGENT_READYTOSHORTCIRCUIT_RETRY_RECOVERY_STRATEGY)"
 * current_version: "V26.50.0"
 * versions: []
 * benchmarks: [
 *   { "name": "technical_accuracy", "test": "Uses concrete technical language", "score": 0.50 },
 *   { "name": "implementation_ready", "test": "Provides clear actionable guidance", "score": 0.50 }
 * ]
 */

import type { PromptPart } from '../../parts/PromptPart';

export const PROMPTPART_SPECIFIC_AGENT_READYTOSHORTCIRCUIT_RETRY_RECOVERY_STRATEGY: PromptPart = 
  'Recovery: re‑evaluate with relaxed thresholds, request missing inputs, or escalate to a human‑in‑the‑loop step before proceeding.' as PromptPart;