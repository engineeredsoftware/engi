// Shared digest-specific utility types and helpers that are used in more than
// one digest sub-package.  This file is intentionally left mostly empty until
// the actual refactor migrates code out of the monolithic implementation.

// When you add a new shared type do it like:
// export interface FileDigest { ... }
