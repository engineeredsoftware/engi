{
  "name": "@bitcode/templates-generics",
  "version": "1.0.0",
  "description": "Templates system types and business logic for the Bitcode platform",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bitcode/supabase": "workspace:*",
    "@bitcode/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
