/**
 * AWS Location MCP underlying utilities – migrated from `uapi/lib/mcps/aws-location.ts`.
 */

export async function awsLocationGeospatialQueryTool(params: { query: string; [key: string]: any }): Promise<any> {
  return { results: [] };
}