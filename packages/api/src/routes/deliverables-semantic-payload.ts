/**
 * Compatibility export for retained completion payload import paths.
 *
 * Active implementation lives in `routes/shippables-semantic-payload.ts`.
 */

export { buildSemanticCompletionResult } from './shippables-semantic-payload';
