{
  "name": "@engi/bitbucket",
  "version": "1.0.0",
  "description": "Bitbucket provider implementation, OAuth, and MCP tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@engi/vcs": "workspace:*",
    "@engi/logger": "workspace:*",
    "@engi/supabase": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}