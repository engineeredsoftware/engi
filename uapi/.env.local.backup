# Local overrides for UI feature flags while investigating performance
# Do NOT commit secrets here. Only safe boolean toggles.

# —— Marketing page toggles ——
NEXT_PUBLIC_COMING_SOON=false
NEXT_PUBLIC_SOFT_LAUNCH=false
NEXT_PUBLIC_CONVERSATION_SECTION=false
NEXT_PUBLIC_INVISIBLE_INTERFACES_GROUP=true
NEXT_PUBLIC_DISABLE_USING=false
NEXT_PUBLIC_LIGHTPAPER_BANNER=false

# —— High-level UI surfaces (turn *everything* off) ——
NEXT_PUBLIC_NOTIFICATIONS_WIDGET=false
NEXT_PUBLIC_CONVERSATIONS_WIDGET=true

NEXT_PUBLIC_SIDEBAR_LEFT=true

# Nav bar – newly added flag
NEXT_PUBLIC_NAV_BAR=true
